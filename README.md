# Mlflow-Codebase
Mlflow-Codebase

Create Environment

```
conda create --prefix ./env python=3.6 -y && conda activate ./env
```

Test Runs Available in mlruns folder

For Mlflow UI 
```
command: mlflow ui
```
